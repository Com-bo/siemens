import React from 'react';
import Home from '@/modules/home';

// export default (props: any) => {
//   return <Home {...props} />;
// };
export default (props: any) => {
  return <div>开发中...</div>;
};
